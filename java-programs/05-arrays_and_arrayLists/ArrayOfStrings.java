package com.gunjana.DsaJava.Arrays_ArrayList;

public class ArrayOfStrings {
    public static void main(String[] args) {
        String[] arr = new String[5];
        System.out.println(arr[2]);     // null (it's a special literal of type null, you cannot create null type)

       // int str = null;               // null cannot be assigned to primitives
        String str = null;              // nul can be assigned to non primitives

        // null is not a datatype, it is the default value initialized
    }
}
